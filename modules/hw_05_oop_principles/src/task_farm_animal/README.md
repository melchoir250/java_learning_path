## 🚜 Ферма: Система Управления Сельскохозяйственными Животными (Farm Management)

### 📝 Обзор Задания

Цель данного задания — спроектировать систему управления фермой, которая может работать с одним сельскохозяйственным животным в определенный момент времени. Система должна уметь вызывать три основных действия (кормление, уход, производство) через общий интерфейс, при этом каждое животное должно демонстрировать **уникальную логику** для каждого действия. Основной акцент делается на **Полиморфизме** и **Инкапсуляции логики ухода и продукции**.

### ✨ Ключевые Требования

1.  **Общий Интерфейс:** Должен быть единый контракт для всех животных (`FarmAnimal`).
2.  **Специфические Действия:** Каждое животное должно иметь уникальную реализацию методов `feed()`, `care()`, и `produce()`.
3.  **Управление:** Класс `Farm` должен управлять текущим животным и вызывать все три действия через общий интерфейс.

### 🏛️ Архитектура и Классы

Система использует **Полиморфизм** и **Инкапсуляцию** для обработки специфических нужд и продукции животных.

| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`FarmAnimal`** | $\text{I}$ (Интерфейс) | **Контракт:** Определяет три обязательных метода: `feed()`, `care()`, `produce()`. |
| **`Cow`** | $\text{C}$ (Класс) | **Корова:** Реализует `FarmAnimal`. Инкапсулирует логику: `feed()` - 'ест траву', `care()` - 'выпас', `produce()` - 'даёт молоко'. |
| **`Chicken`** | $\text{C}$ (Класс) | **Курица:** Реализует `FarmAnimal`. Инкапсулирует логику: `feed()` - 'ест зерно', `care()` - 'нуждается в кормушке', `produce()` - 'несёт яйца'. |
| **`Farm`** | $\text{C}$ (Класс-менеджер) | **Управление:** Хранит поле `animal: FarmAnimal`. Имеет методы `setAnimal()` для установки существа и `serviceAnimal()` для выполнения всех трех действий. |


### 🔑 Принципы ООП в Задании

#### 1. Инкапсуляция Логики

* **Как используется:** Уникальные потребности (`'выпас'`, `'кормушка'`) и продукция (`'молоко'`, `'яйца'`) каждого животного инкапсулированы внутри их конкретных классов (`Cow`, `Chicken`).
* **Результат:** Класс `Farm` не знает и не должен знать, что именно нужно корове или что производит курица.

#### 2. Полиморфизм (Многообразие форм)

* **Как используется:** Класс `Farm` обращается к текущему животному только через общий тип `FarmAnimal`. Метод `serviceAnimal()` вызывает `animal.feed()`, `animal.care()`, и `animal.produce()`.
* **Результат:** Благодаря полиморфизму, эти вызовы автоматически активируют **правильную, специфическую** логику, определенную в текущем объекте (`Cow` или `Chicken`).

### 🖼️ Структура Взаимодействия

1.  **Установка животного:** `Farm.setAnimal(new Cow())` или `Farm.setAnimal(new Chicken())`.
2.  **Обслуживание:** Вызов `Farm.serviceAnimal()` запускает полный цикл ухода, демонстрируя:
    * **Кормление:** Специфическое `feed()`.
    * **Уход:** Специфическое `care()`.
    * **Продукция:** Специфическое `produce()`.

Это делает систему легко расширяемой: добавление, например, класса `Sheep` (Овца) не потребует изменений в классе `Farm`.
