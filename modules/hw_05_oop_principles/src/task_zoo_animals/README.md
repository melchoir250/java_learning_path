# 🐘🐦 Система "Зоопарк": Гибкий Дизайн Поведения

## 🎯 Обзор Задачи

Задача состоит в проектировании системы "Зоопарк", способной работать с различными типами животных (например, Птица, Слон) и демонстрировать их уникальное поведение (звуки и способы передвижения), используя полиморфизм.

## 💡 Выбранный Принцип Дизайна

Для обеспечения максимальной **гибкости** и **расширяемости** использован принцип **Полиморфизма** через **Интерфейс/Абстрактный класс**. Это позволяет классу `Zoo` взаимодействовать с любым животным единообразно, не зная его конкретного типа.

## 🏗️ Архитектурная Структура (UML)

Система основана на четком разделении ролей, как показано на диаграмме:



### 1. Интерфейс/Абстрактный Класс: `Animal`

Определяет общий контракт, который должен соблюдать каждый обитатель зоопарка.

* **Методы:**
    * `makeSound()`: void
    * `move()`: void

### 2. Конкретные Классы Животных: `Bird` и `Elephant`

Эти классы реализуют интерфейс `Animal`, предоставляя специфическое поведение:

| Класс | Реализация `makeSound()` | Реализация `move()` |
| :--- | :--- | :--- |
| **`Bird`** | "Птица чирикает" | "Птица летает" |
| **`Elephant`** | "Слон трубит" | "Слон ходит" |

### 3. Класс-Контейнер: `Zoo`

Класс-менеджер, который хранит и управляет животным через ссылку на общий тип `Animal`.

* **Атрибут:** `animal: Animal`
* **Ключевой Метод:** `showAnimalBehavior()`

## ⭐ Преимущества Дизайна

* **Полиморфизм:** Метод `showAnimalBehavior()` вызывает корректное поведение (`трубит` или `чирикает`) благодаря механизму полиморфизма, что устраняет необходимость в блоках `if/else` для проверки типа животного.
* **Расширяемость:** Добавление нового животного (например, Тигра) требует только создания нового класса, реализующего `Animal`, без необходимости менять код в классе `Zoo`.
* **Слабая Связанность:** Класс `Zoo` зависит только от абстракции `Animal`, что делает систему устойчивой к внутренним изменениям в конкретных классах животных.

## ➕ Расширение Системы

Для добавления нового животного достаточно создать класс, который реализует методы `makeSound()` и `move()` в соответствии с его уникальным поведением, и передать его в `Zoo` через метод `setAnimal()`.