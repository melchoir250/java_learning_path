## 🍽️ Управление Рестораном (Restaurant Menu Management System)

### 📝 Обзор Задания

Задача состоит в проектировании системы управления меню ресторана. Ключевая особенность — необходимость отображать **различные параметры** (температуру, объем) для разных категорий блюд (Горячее блюдо, Напиток) при использовании **единого интерфейса** для всех позиций меню. Система должна использовать интерфейсы и коллекции.

### ✨ Ключевые Требования

1.  **Общий Интерфейс:** Должен быть единый контракт для всех элементов меню.
2.  **Специфические Параметры:** Классы должны инкапсулировать уникальные поля (`temperature`, `volume`).
3.  **Единообразное Описание:** Метод получения описания должен работать для всех блюд, но включать их специфические параметры.
4.  **Управление Меню:** Должен быть класс для хранения коллекции всех блюд и вывода полного меню.

### 🏛️ Архитектура и Классы

Система строится на принципах **полиморфизма** (через общий интерфейс `Dish`) и **инкапсуляции** (в `HotDish` и `Drink`).

| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`Dish`** | $\text{I}$ (Интерфейс) | **Общий Контракт:** Определяет обязательный метод `getDescription()`: String. Все элементы меню должны реализовать этот контракт. |
| **`HotDish`** | $\text{C}$ (Класс) | **Горячее Блюдо:** Реализует `Dish`. Имеет специфическое поле `temperature: int`. `getDescription()` возвращает описание с температурой. |
| **`Drink`** | $\text{C}$ (Класс) | **Напиток:** Реализует `Dish`. Имеет специфическое поле `volume: int`. `getDescription()` возвращает описание с объемом. |
| **`Menu`** | $\text{C}$ (Класс) | **Менеджер Меню:** Хранит коллекцию блюд (`dishes: List<Dish>`). Имеет методы `addDish(d: Dish): void` и `printMenu(): void`. |


### 🔑 Принципы ООП в Задании

#### 1. Полиморфизм (Через Интерфейс `Dish`)

* **Как используется:** Класс `Menu` хранит список объектов типа `Dish` (коллекция `List<Dish>`). При выводе меню, `Menu` перебирает список и вызывает `getDescription()` для каждого элемента.
* **Результат:** Класс `Menu` не знает, является ли текущий объект горячим блюдом или напитком, но благодаря полиморфизму, вызывается **корректный, специфический** метод `getDescription()`, который уже содержит логику отображения температуры или объема.

#### 2. Инкапсуляция Данных

* **Как используется:** Поля `temperature` и `volume` инкапсулированы внутри своих конкретных классов (`HotDish` и `Drink`).
* **Результат:** Каждый класс управляет своими уникальными данными и логикой форматирования описания.

#### 3. Использование Коллекций (`Menu`)

* **Как используется:** Класс `Menu` использует коллекцию (`List<Dish>`) для хранения всех элементов.
* **Результат:** Система легко расширяема, позволяет добавлять любое количество элементов и обрабатывать их единообразно.
