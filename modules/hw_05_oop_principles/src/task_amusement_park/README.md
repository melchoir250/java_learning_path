## 🎡 Парк Развлечений: Система Управления Аттракционами (Amusement Park Management)

### 📝 Обзор Задания

Задача состоит в проектировании системы управления парком развлечений, которая может работать с **одним активным аттракционом**. Система должна предоставлять два ключевых действия:
1.  Вывод **уникальной информации** об аттракционе для посетителей.
2.  Выполнение **специфического технического обслуживания** аттракциона.

Система должна использовать **Полиморфизм** для единообразного взаимодействия с разными типами аттракционов и **Инкапсуляцию** для сокрытия их уникальных особенностей.

### ✨ Ключевые Требования

1.  **Общий Интерфейс:** Должен быть единый контракт (`Attraction`) с методами `info()` и `maintain()`.
2.  **Специфическая Логика:** Классы должны инкапсулировать уникальную информацию и процедуру обслуживания:
    * **`RollerCoaster` (Американские горки):** info $\rightarrow$ 'острые ощущения'; maintain $\rightarrow$ 'проверка безопасности'.
    * **`Carousel` (Карусель):** info $\rightarrow$ 'спокойный аттракцион'; maintain $\rightarrow$ 'тех. обслуживание'.
3.  **Управление:** Класс `AmusementPark` должен управлять текущим аттракционом.

### 🏛️ Архитектура и Классы

Система построена на принципах **Композиции** (`AmusementPark` *содержит* `Attraction`) и **Полиморфизма**.

| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`Attraction`** | $\text{I}$ (Интерфейс) | **Контракт:** Определяет два обязательных метода: `info(): String` и `maintain(): void`. |
| **`RollerCoaster`** | $\text{C}$ (Класс) | **Реализация:** Реализует `Attraction`. Инкапсулирует логику: 'острые ощущения' и 'проверка безопасности'. |
| **`Carousel`** | $\text{C}$ (Класс) | **Реализация:** Реализует `Attraction`. Инкапсулирует логику: 'спокойный аттракцион' и 'тех. обслуживание'. |
| **`AmusementPark`** | $\text{C}$ (Класс-менеджер) | **Управление:** Содержит поле `attraction: Attraction`. Имеет методы `setAttraction()` и `operateAttraction()` для демонстрации работы и обслуживания. |


### 🔑 Принципы ООП в Задании

#### 1. Инкапсуляция и Расширяемость

* **Инкапсуляция:** Уникальные ощущения и процедуры обслуживания полностью скрыты внутри конкретных классов аттракционов.
* **Расширяемость:** Добавление нового аттракциона (например, `FerrisWheel`) требует только создания нового класса, реализующего `Attraction`, без изменения кода `AmusementPark`.

#### 2. Полиморфизм

* **Как используется:** Класс `AmusementPark` обращается к аттракциону только через общий тип `Attraction`. Метод `operateAttraction()` вызывает `attraction.info()` и `attraction.maintain()`.
* **Результат:** При вызове `operateAttraction()`, система выполняет **правильную** логику для текущего аттракциона, будь то техническое обслуживание карусели или проверка безопасности горок.

### 🖼️ Структура Взаимодействия

1.  **Установка аттракциона:** `AmusementPark.setAttraction(new RollerCoaster())`.
2.  **Эксплуатация:** Вызов `AmusementPark.operateAttraction()` запускает:
    * Вывод информации (`attraction.info()`).
    * Выполнение обслуживания (`attraction.maintain()`).

Таким образом, оператор парка использует один и тот же метод для управления всеми аттракционами, а система обеспечивает необходимую специфику.
