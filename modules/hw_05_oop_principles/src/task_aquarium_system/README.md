## 🐠 Аквариум: Система Управления Морскими Существами (Aquarium Management)

### 📝 Обзор Задания

Цель данного задания — спроектировать систему "Аквариум", которая может управлять одним морским существом в определенный момент времени и демонстрировать его уникальное поведение. Основной акцент делается на использовании принципов **Полиморфизма** и **Композиции** для обеспечения гибкости и расширяемости системы.

### ✨ Ключевые Требования

1.  **Общий Интерфейс:** Все морские существа должны соответствовать общему контракту, определяющему их поведение.
2.  **Специфическое Поведение:** Каждое конкретное существо должно иметь свою уникальную реализацию этого поведения.
3.  **Управление:** Класс `Aquarium` должен управлять текущим существом и вызывать его поведение через общий интерфейс.

### 🏛️ Архитектура и Классы

Система основана на **Композиции** (`Aquarium` *имеет* `SeaCreature`) и **Полиморфизме** (обращение к `SeaCreature` через интерфейс).

| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`SeaCreature`** | $\text{I}$ (Интерфейс) | **Контракт:** Определяет обязательное поведение `move(): void`. |
| **`Shark`** | $\text{C}$ (Класс) | **Реализация:** Реализует `SeaCreature`. Поведение: `'быстро и агрессивно'`. |
| **`Starfish`** | $\text{C}$ (Класс) | **Реализация:** Реализует `SeaCreature`. Поведение: `'медленно ползёт по дну'`. |
| **`Aquarium`** | $\text{C}$ (Класс) | **Менеджер:** Хранит поле `creature: SeaCreature` (**Композиция**). Имеет методы `setCreature()` и `showMovement()` для демонстрации поведения. |


### 🔑 Принципы ООП в Задании

#### 1. Композиция (Composition)

* **Как используется:** Класс `Aquarium` содержит (композирует) поле `SeaCreature creature`. Это означает, что аквариум "имеет" морское существо.
* **Результат:** `Aquarium` делегирует выполнение специфического поведения объекту `creature` через метод `showMovement()`.

#### 2. Полиморфизм (Polymorphism)

* **Как используется:** Метод `Aquarium.showMovement()` вызывает `creature.move()`. `Aquarium` не знает, является ли `creature` акулой или морской звездой.
* **Результат:** Благодаря полиморфизму, при вызове `move()` автоматически выполняется **правильная, уникальная** логика движения, инкапсулированная в конкретном классе (`Shark` или `Starfish`).

### 🖼️ Структура Взаимодействия

1.  **Установка существа:** Создается конкретный объект (`new Shark()` или `new Starfish()`) и передается в `Aquarium` через `setCreature()`.
2.  **Демонстрация поведения:** Вызов `Aquarium.showMovement()`:
    * Если установлено `Shark`, выполнится логика `'быстро и агрессивно'`.
    * Если установлено `Starfish`, выполнится логика `'медленно ползёт по дну'`.

Это обеспечивает высокую гибкость: чтобы добавить новый вид существа (например, `Turtle`), достаточно создать новый класс, реализующий `SeaCreature`, без изменения кода `Aquarium`.
