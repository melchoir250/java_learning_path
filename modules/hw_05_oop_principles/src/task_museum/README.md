## 🖼️ Музей: Система Управления Экспонатами (Museum Exhibit Management)

### 📝 Обзор Задания

Задача состоит в проектировании системы, которая управляет **одним активным экспонатом** в музее. Система должна предоставлять два ключевых действия:
1.  **Описать** экспонат (история).
2.  Обеспечить его **сохранение** (условия хранения).

Система должна использовать принципы **Абстракции** и **Композиции** для обеспечения единообразного управления, несмотря на уникальные требования разных типов экспонатов (Манускрипт, Скульптура).

### ✨ Ключевые Требования

1.  **Общий Интерфейс (Абстракция):** Должен быть единый контракт (`Exhibit`) с методами `describe()` и `preserve()`.
2.  **Специфическая Логика:** Классы должны инкапсулировать уникальную историю и процедуру хранения:
    * **`Manuscript` (Манускрипт):** describe $\rightarrow$ 'древний текст'; preserve $\rightarrow$ 'контролируемая влажность'.
    * **`Sculpture` (Скульптура):** describe $\rightarrow$ 'скульптурный объект'; preserve $\rightarrow$ 'реставрация'.
3.  **Управление:** Класс `Museum` должен содержать объект экспоната и управлять его показом и сохранением.

### 🏛️ Архитектура и Классы

Система построена на **Композиции** (`Museum` *содержит* `Exhibit`) и **Полиморфизме**, как показано на схеме.
| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`Exhibit`** | $\text{I}$ (Интерфейс) | **Контракт (Абстракция):** Определяет два обязательных метода: `describe(): String` и `preserve(): void`. |
| **`Manuscript`** | $\text{C}$ (Класс) | **Манускрипт:** Реализует `Exhibit`. Логика: 'древний текст' и 'контролируемая влажность'. |
| **`Sculpture`** | $\text{C}$ (Класс) | **Скульптура:** Реализует `Exhibit`. Логика: 'скульптурный объект' и 'реставрация'. |
| **`Museum`** | $\text{C}$ (Класс-менеджер) | **Управление (Композиция):** Содержит поле `exhibit: Exhibit`. Имеет методы `setExhibit()` и `showExhibit()` для демонстрации и сохранения. |

### 🔑 Принципы ООП в Задании

#### 1. Абстракция и Полиморфизм

* **Абстракция:** Класс `Museum` абстрагируется от деталей, работая только с общим типом `Exhibit`.
* **Полиморфизм:** Метод `Museum.showExhibit()` вызывает `exhibit.describe()` и `exhibit.preserve()`. Система автоматически выполняет **уникальную** логику для текущего объекта (`Manuscript` или `Sculpture`).

#### 2. Композиция

* **Как используется:** Класс `Museum` содержит (композирует) объект `Exhibit`. Это позволяет музею управлять экспонатом, не будучи его частью.
* **Результат:** Обеспечивает гибкость: `Museum` может легко заменить один экспонат на другой.

### 🖼️ Структура Взаимодействия

1.  **Установка экспоната:** Установка объекта (`new Manuscript()` или `new Sculpture()`) в `Museum` через `setExhibit()`.
2.  **Демонстрация:** Вызов `Museum.showExhibit()` запускает полный цикл:
    * Описание (`exhibit.describe()`).
    * Обеспечение сохранения (`exhibit.preserve()`).

Такой подход обеспечивает высокую расширяемость. Добавление нового типа экспоната (например, `Painting`) не потребует изменения кода `Museum`.
