## 🐾 Управление домашними питомцами: Структура и Принципы ООП

### 📝 Описание Задачи

Система должна обеспечивать управление одним активным домашним питомцем. Главное требование — возможность **универсального взаимодействия** с питомцем, при этом сохраняя **специфичность ухода** для каждого вида.

Например:
* **Собака:** $\rightarrow$ Кормление: ест сухой корм; Взаимодействие: гуляет.
* **Кошка:** $\rightarrow$ Кормление: ест влажный корм; Взаимодействие: играет.

### 🏛️ Архитектура и Классы

В основе системы лежат три типа элементов, спроектированных для обеспечения полиморфизма.

| Компонент | Тип (по схеме) | Назначение |
| :--- | :--- | :--- |
| **`Pet`** | $\text{I}$ (Интерфейс) | **Контракт:** Определяет общий набор действий (`feed()`, `interact()`), которые должен уметь выполнять *любой* питомец. |
| **`Dog` и `Cat`** | $\text{C}$ (Конкретные классы) | **Реализация:** Наследуют или реализуют `Pet`. Содержат уникальное, инкапсулированное поведение для каждого действия. |
| **`PetManager`** | $\text{C}$ (Класс-менеджер) | **Управление:** Отвечает за хранение ссылки на текущего питомца (`pet: Pet`) и вызов его общих методов (`handlePet()`). |



### 🔑 Принципы ООП в Задании

#### 1. Полиморфизм (Многообразие форм)

* **Как используется:** `PetManager` обращается к текущему питомцу только через общий тип `Pet`, не зная, это `Dog` или `Cat`.
* **Результат:** Когда `PetManager` вызывает `currentPet.feed()`, система **автоматически** выполняет правильную, специфическую для данного класса реализацию (например, "ест сухой корм" для собаки или "ест влажный корм" для кошки).

#### 2. Инкапсуляция Поведения

* **Как используется:** Логика о том, что именно делает питомец (`'ест сухой корм'`, `'гуляет'`), полностью спрятана и инкапсулирована внутри его собственного класса (`Dog` или `Cat`).
* **Результат:** Детали ухода за собакой не затрагивают код кошки, и наоборот. Это делает систему чистой, легкой для понимания и устойчивой к изменениям.

### 🖼️ Структура Взаимодействия

1.  **Установка питомца:** `PetManager.setPet(new Dog())` или `PetManager.setPet(new Cat())`.
2.  **Запуск ухода:** `PetManager.handlePet()` вызывает:
    * `currentPet.feed()`
    * `currentPet.interact()`
