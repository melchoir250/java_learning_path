## 🌿 Ботанический Сад: Система Управления Уходом за Растениями (Botanical Garden Management)

### 📝 Обзор Задания

Задача состоит в проектировании системы, управляющей уходом за **одним** активным растением в ботаническом саду. Ключевой момент — использование **абстракции** и **полиморфизма** для обеспечения правильного, специфического ухода для каждого вида растений через единый управляющий механизм.

### ✨ Ключевые Требования

1.  **Общий Интерфейс (Абстракция):** Должен быть единый контракт для всех растений.
2.  **Специфический Уход:** Каждый класс растения должен инкапсулировать и реализовать уникальные требования к уходу.
3.  **Управление:** Класс-менеджер должен содержать объект общего типа и вызывать для него метод ухода.

### 🏛️ Архитектура и Классы

Система использует **Полиморфизм** и **Абстракцию** для обработки специфических потребностей растений.

| Компонент | Тип (по схеме) | Назначение и Особенности |
| :--- | :--- | :--- |
| **`Plant`** | $\text{I}$ (Интерфейс) | **Контракт (Абстракция):** Определяет обязательный метод ухода `care(): void`. |
| **`Orchid`** | $\text{C}$ (Класс) | **Реализация:** Реализует `Plant`. Логика ухода: 'высокая влажность и тень'. |
| **`Cactus`** | $\text{C}$ (Класс) | **Реализация:** Реализует `Plant`. Логика ухода: 'свет и редкий полив'. |
| **`BotanicalGarden`** | $\text{C}$ (Класс-менеджер) | **Управление:** Содержит поле `plant: Plant` и метод `maintainPlant()` для запуска процедуры ухода. |


### 🔑 Принципы ООП в Задании

#### 1. Абстракция

* **Как используется:** Интерфейс `Plant` абстрагирует понятие "растения" до одного действия — `care()`.
* **Результат:** Класс `BotanicalGarden` работает с общим, абстрактным типом `Plant`, не вдаваясь в конкретные детали ухода.

#### 2. Полиморфизм

* **Как используется:** Метод `BotanicalGarden.maintainPlant()` вызывает `plant.care()`.
* **Результат:** Вызов `care()` автоматически выполняет **уникальную** логику ухода, специфичную для установленного в данный момент объекта (`Orchid` или `Cactus`).

### 🖼️ Структура Взаимодействия

1.  **Добавление растения:** Установка объекта (`new Orchid()` или `new Cactus()`) в `BotanicalGarden` через `setPlant()`.
2.  **Обеспечение ухода:** Вызов `BotanicalGarden.maintainPlant()` запускает правильный цикл ухода:
    * Если установлено `Orchid`, выполнится логика `'влажность и тень'`.
    * Если установлен `Cactus`, выполнится логика `'свет и редкий полив'`.
