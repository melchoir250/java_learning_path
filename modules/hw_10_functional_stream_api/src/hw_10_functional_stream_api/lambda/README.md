
# 🧠 Lambda-выражения

## 📝 Задания

### Задача 02: Анонимный Runnable
**Класс:** `Task02_AnonymousRunnable`

Создайте `Runnable` с помощью лямбда-выражения. Запустите поток, который выводит "Поток запущен".

---

### Задача 03: Predicate для чётных чисел
**Класс:** `Task03_PredicateEven`

Создайте `Predicate<Integer>`, который проверяет, является ли число чётным. Проверьте на нескольких числах.

---

### Задача 04: Function для длины строки
**Класс:** `Task04_FunctionStringLength`

Создайте `Function<String, Integer>`, который принимает строку и возвращает её длину. Протестируйте на разных строках.

---

### Задача 05: Consumer для вывода
**Класс:** `Task05_ConsumerPrint`

Создайте `Consumer<String>`, который выводит строку в консоль в верхнем регистре. Проверьте на нескольких строках.

## 🔑 Ключевые интерфейсы

| Интерфейс | Сигнатура | Назначение |
|-----------|-----------|------------|
| `Runnable` | `void run()` | Выполнение действия |
| `Predicate<T>` | `boolean test(T t)` | Проверка условия |
| `Function<T,R>` | `R apply(T t)` | Преобразование |
| `Consumer<T>` | `void accept(T t)` | Потребление значения |

