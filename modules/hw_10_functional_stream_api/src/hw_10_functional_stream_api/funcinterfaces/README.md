# 🔢 Функциональные интерфейсы

## 📝 Задание

Создайте собственный функциональный интерфейс и продемонстрируйте его использование.

---

## ✨ Требования

### 1. Функциональный интерфейс `MathOperation`
- **Метод:** `int operate(int a, int b)`
- **Аннотация:** `@FunctionalInterface`

### 2. Демонстрационный класс `Task01_MathOperationDemo`
Реализовать через лямбда-выражения:
- **Сложение** (`(a, b) -> a + b`)
- **Вычитание** (`(a, b) -> a - b`)
- **Умножение** (`(a, b) -> a * b`)
- **Деление** (`(a, b) -> a / b`)

---

## 🏛️ Структура

```
src/hw_10_functional_stream_api/funcinterfaces/
├── MathOperation.java
└── Task01_MathOperationDemo.java
```

```java
@FunctionalInterface
public interface MathOperation {
    int operate(int a, int b);
}
```

## 🔑 Ключевые понятия

| Термин | Описание |
|--------|----------|
| **Функциональный интерфейс** | Интерфейс с одним абстрактным методом |
| **Лямбда-выражение** | Компактная реализация функционального интерфейса |
| **@FunctionalInterface** | Маркер для проверки компилятором |

---