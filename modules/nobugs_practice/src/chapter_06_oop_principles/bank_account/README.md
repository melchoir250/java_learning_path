# 🏦 Банковский счёт: Реализация Инкапсуляции

## 📝 Задание

Создайте класс `BankAccount`, который реализует принцип **инкапсуляции** для защиты финансовых данных. Класс должен обеспечивать безопасное управление балансом счёта через контролируемый интерфейс методов.

## ✨ Требования

### 1. Защита данных
- Поле `balance` должно быть приватным (`private`)
- Прямой доступ к полю `balance` извне класса должен быть невозможен

### 2. Контролируемый интерфейс
Реализуйте следующие публичные методы:

#### Метод `deposit(double amount)`
- Назначение: внесение средств на счёт
- Проверка: сумма должна быть положительной
- Действие: увеличение баланса на указанную сумму

#### Метод `withdraw(double amount)`
- Назначение: снятие средств со счёта
- Проверки:
    1. Сумма должна быть положительной
    2. Сумма не должна превышать текущий баланс
- Действие: уменьшение баланса на указанную сумму

#### Метод `getBalance()`
- Назначение: получение текущего баланса
- Возвращает: значение баланса (только для чтения)

## 🏛️ Структура класса

```java
public class BankAccount {
    // Приватное поле для хранения баланса
    private double balance;
    
    // Конструктор (опционально)
    
    // Метод для внесения средств
    public void deposit(double amount) {
        // Реализация с проверкой
    }
    
    // Метод для снятия средств
    public void withdraw(double amount) {
        // Реализация с проверками
    }
    
    // Метод для получения баланса
    public double getBalance() {
        // Реализация
    }
}
```

## 🔑 Ключевые принципы

### Инкапсуляция
- Скрытие внутреннего состояния объекта
- Доступ к данным только через публичные методы
- Защита целостности данных через валидацию

### Безопасность
- Предотвращение некорректных операций
- Исключение возможности отрицательного баланса
- Контроль всех операций с балансом

## 📋 Что проверить в реализации

1. [ ] Поле `balance` объявлено как `private`
2. [ ] Метод `deposit()` проверяет положительность суммы
3. [ ] Метод `withdraw()` проверяет:
    - [ ] Положительность суммы
    - [ ] Достаточность средств
4. [ ] Метод `getBalance()` возвращает значение без возможности изменения
5. [ ] Нет публичных сеттеров для поля `balance`
6. [ ] Все операции с балансом выполняются только через методы класса

## 🎯 Цель задания

Понять и применить принцип инкапсуляции на практике:
- Защитить внутренние данные класса
- Создать безопасный интерфейс для работы с объектом
- Обеспечить целостность данных через валидацию операций

---

**Примечание:** Не используйте статические поля или методы, так как они нарушают принцип инкапсуляции для конкретного экземпляра счёта. Каждый объект `BankAccount` должен управлять своим собственным балансом.