# 🎮 Игровой персонаж с управлением здоровьем: Инкапсуляция + Защита данных

## 📝 Задание

Создайте класс игрового персонажа `Player`, который демонстрирует принцип **инкапсуляции** с дополнительной **валидацией данных**. Класс должен обеспечивать безопасное управление здоровьем персонажа в строго определённых пределах.

## ✨ Требования

### 1. Защищённое поле здоровья
- **Поле:** `health` (целочисленное)
- **Доступ:** Приватный (`private`)
- **Запрет:** Прямая установка значения извне класса

### 2. Ограничения здоровья
- **Минимальное значение:** 0
- **Максимальное значение:** 100
- **Начальное значение:** По умолчанию 100 (или настраиваемое в пределах 0-100)

### 3. Методы управления здоровьем

#### Метод `takeDamage(int damage)`
- **Назначение:** Уменьшение здоровья при получении урона
- **Проверки:**
    1. Урон должен быть положительным числом
    2. Результат не должен быть меньше 0
- **Действие:** Уменьшает здоровье на указанный урон, но не ниже 0

#### Метод `heal(int amount)`
- **Назначение:** Увеличение здоровья при лечении
- **Проверки:**
    1. Количество здоровья должно быть положительным
    2. Результат не должен превышать 100
- **Действие:** Увеличивает здоровье на указанное количество, но не выше 100

#### Метод `getHealth()`
- **Назначение:** Получение текущего значения здоровья
- **Возвращает:** Текущее значение здоровья (только для чтения)

### 4. Дополнительные требования
- **Защита от некорректных данных:** Все операции должны проверять корректность входных данных
- **Состояние объекта:** После любой операции здоровье должно оставаться в пределах 0-100
- **Обратная связь:** Методы могут возвращать фактическое изменение здоровья или подтверждение операции

## 🏛️ Структура класса

```java
public class Player {
    // Приватное поле здоровья с ограничением
    private int health;
    
    // Конструкторы
    public Player() {
        this.health = 100; // Начальное значение по умолчанию
    }
    
    public Player(int initialHealth) {
        // Валидация начального значения
        if (initialHealth < 0 || initialHealth > 100) {
            // Обработка некорректного значения
        }
        this.health = initialHealth;
    }
    
    // Метод получения урона
    public void takeDamage(int damage) {
        // Реализация с проверками
    }
    
    // Метод лечения
    public void heal(int amount) {
        // Реализация с проверками
    }
    
    // Геттер для здоровья
    public int getHealth() {
        return health;
    }
}
```

## 🔑 Ключевые принципы

### Инкапсуляция (Encapsulation)
- Сокрытие внутреннего состояния объекта
- Контролируемый доступ через публичные методы
- Защита от некорректных изменений

### Валидация данных (Data Validation)
- Проверка входных параметров перед выполнением операций
- Сохранение целостности объекта
- Защита от ошибочных состояний

### Инварианты класса (Class Invariants)
- Здоровье всегда в пределах 0-100
- Любая операция сохраняет это условие
- Предсказуемое поведение объекта

## 📋 Что проверить в реализации

1. [ ] Поле `health` объявлено как `private`
2. [ ] Нет публичных сеттеров для прямого изменения `health`
3. [ ] Метод `takeDamage()` включает проверки:
    - [ ] Урон положительный
    - [ ] Результат не ниже 0
    - [ ] Если урон превышает текущее здоровье, здоровье устанавливается в 0
4. [ ] Метод `heal()` включает проверки:
    - [ ] Количество лечения положительное
    - [ ] Результат не выше 100
    - [ ] Если лечение превышает лимит, здоровье устанавливается в 100
5. [ ] Метод `getHealth()` возвращает значение без возможности изменения
6. [ ] Конструкторы валидируют начальное значение здоровья
7. [ ] После любой операции здоровье остаётся в пределах 0-100

## 💡 Возможные сценарии использования

```java
Player player = new Player();
System.out.println(player.getHealth()); // 100

player.takeDamage(30);
System.out.println(player.getHealth()); // 70

player.heal(20);
System.out.println(player.getHealth()); // 90

player.takeDamage(100); // Должно установить здоровье в 0
System.out.println(player.getHealth()); // 0

player.heal(150); // Должно установить здоровье в 100
System.out.println(player.getHealth()); // 100

player.takeDamage(-10); // Не должно изменять здоровье (некорректный урон)
player.heal(-5); // Не должно изменять здоровье (некорректное лечение)
```

## 🎯 Цель задания

Понять и применить на практике:

1. **Строгую инкапсуляцию** - полное сокрытие внутреннего состояния
2. **Защиту от некорректных данных** - валидация всех входных параметров
3. **Поддержку инвариантов** - гарантии корректного состояния объекта
4. **Безопасный интерфейс** - методы, которые защищают объект от ошибок использования
5. **Предсказуемость поведения** - объект всегда в допустимом состоянии

---

**Примечание:** Для дополнительной безопасности можно добавить логирование операций, исключения для некорректных параметров или методы для проверки состояния (например, `isAlive()` возвращает `true` если здоровье > 0). Эти улучшения демонстрируют профессиональный подход к проектированию классов.