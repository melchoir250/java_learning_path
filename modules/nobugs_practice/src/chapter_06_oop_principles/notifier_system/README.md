# 📱 Уведомления в системе: Полиморфизм через интерфейс

## 📝 Задание

Создайте систему отправки уведомлений, демонстрирующую принцип **полиморфизма** через интерфейс. Система должна обеспечивать единообразную отправку сообщений через различные каналы связи.

## ✨ Требования

### 1. Интерфейс `NotificationSender`
- **Метод:** `send(String message)` - отправка сообщения

### 2. Классы-реализации интерфейса
Создайте три класса, реализующих интерфейс `NotificationSender`:

#### Класс `EmailSender` (Отправка по email)
- **Реализовать:** `send(String message)` → отправка сообщения по электронной почте

#### Класс `SmsSender` (Отправка по SMS)
- **Реализовать:** `send(String message)` → отправка SMS-сообщения

#### Класс `PushSender` (Push-уведомления)
- **Реализовать:** `send(String message)` → отправка push-уведомления

### 3. Метод для массовой отправки
- **Метод:** `notifyUsers(List<NotificationSender> senders, String msg)`
- **Назначение:** Отправляет сообщение через все предоставленные отправители
- **Параметры:**
    - `senders` - список объектов, реализующих интерфейс `NotificationSender`
    - `msg` - сообщение для отправки
- **Действие:** Вызывает метод `send(msg)` для каждого отправителя в списке

## 🏛️ Структура классов

```java
// Интерфейс
public interface NotificationSender {
    void send(String message);
}

// Классы-реализации (пример для EmailSender)
public class EmailSender implements NotificationSender {
    @Override
    public void send(String message) {
        // Реализация отправки по email
        System.out.println("Отправка email: " + message);
    }
}

// Другие классы аналогично
public class SmsSender implements NotificationSender {
    @Override
    public void send(String message) {
        // Реализация отправки SMS
        System.out.println("Отправка SMS: " + message);
    }
}
```

## 🔑 Ключевые принципы

### Полиморфизм через интерфейс
- Единый контракт для различных реализаций
- Возможность работы с объектами через общий тип интерфейса
- Замена реализации без изменения клиентского кода

### Слабая связность (Low Coupling)
- Зависимость от абстракции (интерфейса), а не от конкретных классов
- Легкость добавления новых типов отправителей
- Возможность динамической замены реализаций

### Принцип открытости/закрытости (Open/Closed Principle)
- Система открыта для расширения (новых типов отправителей)
- Закрыта для модификации (не требуется менять существующий код)

## 📋 Что проверить в реализации

1. [ ] Интерфейс `NotificationSender` объявлен с методом `send(String message)`
2. [ ] Классы `EmailSender`, `SmsSender`, `PushSender` реализуют интерфейс `NotificationSender`
3. [ ] Каждый класс имеет свою уникальную реализацию метода `send()`:
    - [ ] `EmailSender` → логика отправки email
    - [ ] `SmsSender` → логика отправки SMS
    - [ ] `PushSender` → логика отправки push-уведомлений
4. [ ] Использована аннотация `@Override` для методов реализации
5. [ ] Метод `notifyUsers` реализован корректно:
    - [ ] Принимает `List<NotificationSender>` как параметр
    - [ ] Вызывает `send(msg)` для каждого элемента списка
    - [ ] Обрабатывает все отправители в цикле
6. [ ] Можно создать список разных отправителей и передать в `notifyUsers`

## 💡 Пример использования

```java
public class NotificationService {
    public static void notifyUsers(List<NotificationSender> senders, String msg) {
        for (NotificationSender sender : senders) {
            sender.send(msg);
        }
    }
}

// В основном коде
List<NotificationSender> senders = new ArrayList<>();
senders.add(new EmailSender());
senders.add(new SmsSender());
senders.add(new PushSender());

NotificationService.notifyUsers(senders, "Важное уведомление!");
```

## 🎯 Цель задания

Понять и применить принципы ООП:

1. **Полиморфизм через интерфейс** - работа с разными объектами через общий контракт
2. **Инверсия зависимостей** - зависимость от абстракций, а не от конкретных классов
3. **Расширяемость системы** - возможность легко добавить новые типы уведомлений
4. **Повторное использование кода** - метод `notifyUsers` работает с любыми реализациями интерфейса

---

**Примечание:** В реальном приложении методы `send()` будут содержать реальную логику отправки (работа с SMTP для email, API оператора для SMS, Firebase для push и т.д.). В учебных целях достаточно вывода в консоль с указанием типа отправки.