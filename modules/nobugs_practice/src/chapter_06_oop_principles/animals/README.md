# 🐾 Животные и поведение: Наследование и полиморфизм

## 📝 Задание

Создайте иерархию классов животных, демонстрирующую принципы **наследования** и **полиморфизма**. Система должна позволять разным типам животных иметь уникальное поведение при использовании общего интерфейса.

## ✨ Требования

### 1. Базовый класс `Animal`
- **Поле:** `name` (String) - имя животного
- **Метод:** `makeSound()` - издать звук (абстрактный или базовый метод)
- **Метод:** `describe()` - описание животного, которое использует `makeSound()`

### 2. Подклассы-наследники
Создайте три класса, наследующих от `Animal`:

#### Класс `Dog` (Собака)
- **Переопределить:** `makeSound()` → возвращает специфичный для собаки звук

#### Класс `Cat` (Кот)
- **Переопределить:** `makeSound()` → возвращает специфичный для кота звук

#### Класс `Cow` (Корова)
- **Переопределить:** `makeSound()` → возвращает специфичный для коровы звук

### 3. Метод `describe()`
- Должен быть реализован в базовом классе `Animal`
- Использовать метод `makeSound()` для формирования описания
- Пример вывода: `"[Имя животного] говорит: [звук животного]"`

## 🏛️ Структура классов

```java
// Базовый класс
public class Animal {
    protected String name;
    
    // Конструктор
    public Animal(String name) {
        this.name = name;
    }
    
    // Метод для издания звука (может быть абстрактным)
    public String makeSound() {
        return "издаёт звук";
    }
    
    // Метод описания, использующий makeSound()
    public void describe() {
        // Реализация
    }
    
    // Геттер для имени (опционально)
    public String getName() {
        return name;
    }
}

// Подклассы (пример для Dog)
public class Dog extends Animal {
    public Dog(String name) {
        super(name);
    }
    
    @Override
    public String makeSound() {
        return "Гав-гав!";
    }
}
```

## 🔑 Ключевые принципы

### Наследование (Inheritance)
- Создание иерархии "общее → частное"
- Переиспользование кода из базового класса
- Расширение функциональности в подклассах

### Полиморфизм (Polymorphism)
- Разные реализации одного метода в разных классах
- Вызов правильной реализации в зависимости от типа объекта
- Использование общего типа `Animal` для работы с разными животными

### Переопределение методов (Method Overriding)
- Замена реализации метода из родительского класса
- Сохранение сигнатуры метода (имя, параметры, возвращаемый тип)
- Использование аннотации `@Override`

## 📋 Что проверить в реализации

1. [ ] Базовый класс `Animal` содержит поле `name`
2. [ ] Метод `makeSound()` объявлен в базовом классе
3. [ ] Метод `describe()` реализован в базовом классе и использует `makeSound()`
4. [ ] Классы `Dog`, `Cat`, `Cow` наследуются от `Animal`
5. [ ] Каждый подкласс переопределяет метод `makeSound()`:
    - [ ] `Dog` → уникальный звук собаки
    - [ ] `Cat` → уникальный звук кота
    - [ ] `Cow` → уникальный звук коровы
6. [ ] Использована аннотация `@Override` для переопределяемых методов
7. [ ] Конструкторы подклассов вызывают конструктор родителя через `super()`

## 💡 Пример использования

```java
Animal dog = new Dog("Бобик");
dog.describe(); // "Бобик говорит: Гав-гав!"

Animal cat = new Cat("Мурзик");
cat.describe(); // "Мурзик говорит: Мяу!"

Animal cow = new Cow("Бурёнка");
cow.describe(); // "Бурёнка говорит: Мууу!"
```

## 🎯 Цель задания

Понять и применить принципы ООП:
- **Наследование** для создания иерархии классов
- **Полиморфизм** для единообразной работы с разными типами объектов
- **Переопределение методов** для специализации поведения
- **Инкапсуляция** через общий интерфейс базового класса

---

**Примечание:** Обратите внимание на использование `protected` для поля `name` в базовом классе. Это обеспечивает доступ для подклассов, но защищает от прямого доступа извне.